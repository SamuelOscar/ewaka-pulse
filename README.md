# Ewaka-Pulse

Community Management System for Testimony Africa NGO — Africa Ewaka Village.

## Stack
- **Backend:** Python + FastAPI
- **Frontend:** React + Vite + TailwindCSS
- **Database:** PostgreSQL (Neon.tech)
- **Auth:** JWT + bcrypt

## Setup

### Backend
```bash
cd backend
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

### Frontend
```bash
cd frontend
npm install
npm run dev
```

## Developer
Samuel — Volunteer Engineer, Testimony Africa NGO